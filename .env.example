# Variables de entorno del Frontend.

# URL del backend
# http://localhost:3000
# https://api-dominio.com
VITE_API_URL=http://localhost:3000

# URL del servidor de WebSocket/Socket.io
# Ejemplos: http://localhost:3000 o https://api-dominio.com
VITE_SOCKET_URL=http://localhost:3000

# Estrategia de autenticación:
# - token  : el frontend envía Authorization: Bearer <token>
# - cookie : el backend gestiona sesión con cookie HttpOnly (lo realizo para producción)
VITE_AUTH_STRATEGY=token

# Almacenamiento de token cuando AUTH_STRATEGY=token
# Valores: 
# - local   : persiste entre sesiones (más cómodo, menos seguro)
# - session : persiste hasta cerrar pestaña/ventana
# - memory  : no persiste (más seguro, se pierde al recargar)
VITE_AUTH_STORAGE=local
